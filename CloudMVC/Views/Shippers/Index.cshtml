@model IEnumerable<CloudMVC.Shippers>

@{
    ViewBag.Title = "Shippers";
}

<p>
    <a href="@Url.Action("Create", "Shippers")" class="btn btn-success"><span>Create New</span></a>
</p>

<table class="table table-bordered table-striped table-vcenter">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShipperName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShipperName)
            </td>
            <td>               
                <div class="btn-group btn-group-xs">
                    <a href="@Url.Action("Edit", "Shippers", new { id = item.ShipperID })" class="btn btn-sm btn-warning" title="Edit"><i class="fa fa-pencil"></i></a>

                    <a href="@Url.Action("Details", "Shippers", new { id = item.ShipperID })" class="btn btn-sm btn-info" title="Details"><i class="fa fa-eye"></i></a>

                    <a href="@Url.Action("Delete", "Shippers", new { id = item.ShipperID })" class="btn btn-sm btn-danger" title="Delete"><i class="fa fa-times"></i></a>
                </div>
            </td>
        </tr>
    }

</table>